import { Button } from "@mui/material"
import { Link } from "react-router-dom"

function Channel() {
  return (
    <Link to={'/'}>
      <Button>Main</Button>
    </Link>
  )
}

export default Channel